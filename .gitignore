# Secrets -- never commit a real API key
.env

# Python
__pycache__/
*.pyc
.venv/

# The Chinook DB is regenerated by setup.sh; don't commit the binary
data/Chinook.db

# OS cruft
.DS_Store

# Personal scratch script -- not part of the deliverable, superseded by
# generate_dev_answers.py and compare_models.py
scratch_test.py

# Raw stdout captures -- redundant with the structured JSON the scripts
# themselves write (dev_answers.json, model_comparison.json)
*.log
